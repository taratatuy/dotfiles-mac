# scrollback size
set -g history-limit 10000

set -g base-index 1
set -g pane-base-index 1
bind '`' move-window -r

bind-key a switch-client -Ttable1
bind-key -Ttable1 1 pipep -I '/bin/zsh ~/d/tmux-test.sh'

set -s focus-events on
set -s extended-keys on
set -s escape-time 0

set -g mouse on
